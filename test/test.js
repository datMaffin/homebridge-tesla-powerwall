/* eslint-env mocha */
var assert = require('assert');

describe('Utilites', function() {
    describe('#_httpGetRequest(url, callback)', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
    describe('#_parseJSON(str)', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
    describe('ValueGetter', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
    describe('#reset(service, characteristic, getterByCallback, delay)', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
    describe('Polling', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
});
describe('Accessories', function() {
    describe('Powerwall', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
    describe('PowerMeter', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
});
describe('Platform', function() {
    describe('TeslaPowerwall', function() {
        it('should succeed', function() {
            assert.equal(0, 0);
        });
    });
});
